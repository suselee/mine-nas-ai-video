# Person filter dependencies for Armbian.
# Install:  pip install -r requirements-filter.txt
# Total download: ~30MB (opencv wheel + flask + numpy)
opencv-python-headless>=4.8.0
flask>=3.0.0
numpy>=1.24.0
